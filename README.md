# Frontend-works

This repository contains small frontend demos and exercises.

Files:
- `array.html` — a page demonstrating JavaScript arrays
- `array.js` — script demonstrating array creation, push/pop, iteration, map/filter, reduce, and DOM output

Open `array.html` in a browser to see the demo.